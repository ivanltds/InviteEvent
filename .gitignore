

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/
.next/
out/
.swc/

# Testing
coverage/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
# Sessão real de teste gerada pelo Playwright (tests/auth.setup.ts) — chegou
# a ser commitada por engano (docs/analise/05-privacidade-e-higiene-repo.md,
# HIG-04); nunca deve ser versionada, contém token de sessão real.
/tests/.auth/

# Temp test and backup files
test_results*.txt
*.sql
!supabase/migrations/*.sql
!supabase/seed.sql
build-log.txt
