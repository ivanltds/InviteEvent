// Mock para arquivos de estilo no Jest
// Adicionado teste dummy para evitar erro de suíte vazia
describe('Style Mock', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});

module.exports = {};
